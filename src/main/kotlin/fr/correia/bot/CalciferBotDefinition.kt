package fr.correia.bot

import ai.tock.bot.definition.bot

const val botIdName = "calcifer"
const val botNamespace = "correia"

val calciferBot = bot(
    botIdName,
    namespace = botNamespace,
    stories = emptyList()
)